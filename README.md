# objects-and-data-abstraction-final-project

Before running this, make sure to install all needed dependencies:

```
pip install pygame
pip install cairosvg
```

Then, you can run the launcher using:

`python launcher/src/main.py`
